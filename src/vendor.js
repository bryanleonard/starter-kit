/* Contains references to vendor library being used. this is used by webpack in the production only build */

/* eslint-disable no-unused-vars */
import fetch from 'whatwg-fetch';
import $ from 'jquery';
